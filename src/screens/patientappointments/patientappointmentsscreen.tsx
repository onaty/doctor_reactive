// In App.js in a new project

import * as React from 'react';
import { View, Text } from 'react-native';

export const PatientappointmentsScreen = () => {
    return (
        <View style={[]}>
            <Text style={[]}>Patient appointments Screen</Text>
        </View>
    );
}
