// In App.js in a new project

import * as React from 'react';
import { View, Text } from 'react-native';

export const PatientmessagesScreen = () => {
    return (
        <View style={[]}>
            <Text style={[]}>Patient messages screen</Text>
        </View>
    );
}
